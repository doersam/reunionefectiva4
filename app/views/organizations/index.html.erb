<h1>Todas las Organizaciones</h1>
<% @organizations.each do |org|%>
  <h3><%=link_to org.name, organization_path(org)%></h3>
  <p>Cedula Juridica: <%= org.legal_id%></p>
  <p>Contacto: <%= org.contact_email %></p>
<% end %>
